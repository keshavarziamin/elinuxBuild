name: assignment-test
on: [push]
jobs:
    build:
        # container: elinux/crosstoolng:build
        runs-on: self-hosted
        steps:
          - uses: actions/checkout@v2
          - name: build crosstollng
            run: ./crosstool-NG.sh